services:
  node-server:
    build:
      context: .
    image: "maainul/test-app:latest"
    ports:
      - "8000:8000"
  prom-server:
    image: prom/prometheus
    ports:
      - 9090:9090
    volumes:
      - ./prometheus-config.yml:/etc/prometheus/prometheus-config.yml
  grafana-server:
    image: grafana/grafana-oss
    ports:
      - 3000:3000
